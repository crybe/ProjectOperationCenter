services:
  tactical-dashboard:
    build: .
    container_name: tactical-dashboard
    restart: unless-stopped
    ports:
      - "5666:5000"
    volumes:
      - .:/app
      - ./data:/data
      # - /var/run/docker.sock:/var/run/docker.sock # Optional: if you want to monitor local docker
    extra_hosts:
      - "host.docker.internal:host-gateway"
    env_file:
      - .env
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:5000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
